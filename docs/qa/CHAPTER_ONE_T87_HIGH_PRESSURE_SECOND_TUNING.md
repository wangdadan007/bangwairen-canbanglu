# T87 两条高压线二次调平记录

日期：2026-05-24

## 来源

T86 调平后保护复测显示：

- 照微裂榜塞卷商店线仍在 Boss 前失败收束，商店后会被多敌、塞卷 / 污卷与后段支援连续压穿。
- 衡简归册事件商店线能抵达 Boss，但火部迟审、污牒收束和 Boss 连续消耗仍偏紧。
- 莲烬稳行商店线已能通关，本轮不削稳行线经济，也不抬高 Boss 形。

用户要求 T87 只做两条高压线二次调平，优先处理裂榜商店线 Boss 前修形 / 香封窗口，以及归册事件商店线 Boss 前连续压力。

## 调整

| 路线 | T87 调整 | 目的 |
|---|---|---|
| 裂榜商店线 | `终审前残页` 后新增休整节点 `残页补香`，再进入 `逃名压卷`。 | 让低己形角色在最后遮名 / 支援压力前有一次稳定修形窗口，并让已购香封在终审前有整理空间。 |
| 裂榜商店线 | `终审前残页` 事件池移除 `污页裹扎`，保留遮名、法宝、三坛与朱批相关补给。 | 避免商店后继续叠污卷风险，减少“买了补给但马上被牌堆污染打穿”的窄口。 |
| 归册事件商店线 | `污牒收束` 遭遇池移除 `塞卷吏` 和 `瘟纸人与供桌残像`，改为瘟纸人复战、供桌残像复战或供桌 + 偷香鼠压力。 | 降低火部迟审后的连续塞卷 / 多敌峰值，保留污染与支援考题但不再连续压穿。 |

## 边界

- 不新增生产依赖、卡牌、敌人、遭遇模板、香封、法宝、Boss、章节、角色、正式素材或音频。
- 不改变第一章 48 张卡牌上限、香封 2 槽边界、伏诛 / 归册奖励同池、前三场固定教学单敌节奏或 Boss 形。
- 不处理完整随机地图、Ascension / Daily / Custom、完整角色专属牌池或 run 后图鉴 / 终册叙事解锁。

## 验证记录

| 命令 / 检查 | 结果 |
|---|---|
| `npm run test -- src/tests/routeResolver.test.ts` | 通过，1 个测试文件、9 个测试。 |
| `npm run test -- src/tests/data.test.ts src/tests/routeResolver.test.ts src/tests/chapterOneLongFlow.test.ts` | 首次运行失败，因为 `data.test.ts` 仍按旧口径要求裂榜商店线事件池包含 `污页裹扎`；已更新测试基线后重跑通过，3 个测试文件、31 个测试。 |
| `npm run typecheck` | 通过。 |
| `npm run lint` | 通过。 |
| `git diff --check` | 通过。 |
| `npm run build` | 通过；Vite 仍提示首包 chunk 超过 500 kB，沿用既有 warning。 |

## 调平后保护复测

复测日期：2026-05-24

方式：本地 Vite 页面真实浏览器流程。普通沙箱启动 `npm run dev -- --host 127.0.0.1 --port 5173` 因 `listen EPERM 127.0.0.1:5173` 失败，提权后启动成功。复测只走用户指定的两条线，不扩大到稳行线、秘传稀有度或外层模式。

| 路线 | 复测口径 | 结果 | 观察 |
|---|---|---|---|
| 照微裂榜商店线 | 生存优先：前三战和前段可归册，后段优先香封、封势、断异动和快速伏诛。 | 失败收束 7 / 9，未抵达 Boss。结算：伏诛 2、归册 5、休整 1、商店 3、香封消耗 1、己形 0 / 64。 | `残页补香` 能把照微从商店后低己形拉回一段，样本曾推进到 `斗部空壳` 前 32 / 64；但 `逃名压卷` 后再接 `斗部空壳` 仍偏窄，低己形角色没有稳定 Boss 前余量。 |
| 衡简归册事件商店线 | 归册倾向：保留问名 / 登簿收益，同时在后段使用香封与 Boss 前休整。 | 通关 9 / 9，Boss 归册。结算：伏诛 1、归册 8、休整 1、商店 3、香封消耗 3、己形 34 / 72。 | `污牒收束` 遭遇池收敛后不再连续塞卷压穿；`终审前校册` 休整窗口有效，Boss 前后仍紧，但已可完成。 |

## 追加小调与复跑

用户追加要求：继续在 T87 内小调照微裂榜商店线的 Boss 前连续压力 / 香封补给下限，不扩大到稳行线、秘传稀有度或外层挑战模式。

追加调整：

| 调整点 | 内容 | 边界 |
|---|---|---|
| 裂榜商店线香封下限 | `碎页暗市` 只在 `route_node_fracture_shop` 提供 2 个 seeded 香封候选；普通商店仍保持 1 个香封候选。 | 不增加香封槽位，不新增香封定义，不改稳行线商店。 |
| `逃名压卷` 连续压力 | 遭遇池移除 `逃名纸马 + 香灰虱` 多敌组合，改入单体逃名纸马复战，保留供桌 + 偷香鼠、香灰虱等轻支援压力。 | 不新增遭遇模板，不改 Boss 形，不改斗部空壳。 |

追加复跑：

| 路线 | 复跑口径 | 结果 | 观察 |
|---|---|---|---|
| 照微裂榜商店线 | 生存优先的真实浏览器流程，明确走 `断签判命 -> 塞卷旧案 -> 碎页暗市`。 | 通关 9 / 9，Boss 归册。结算：休整 2、商店 3、事件 1、法宝 3 件、裁定 9 次、己形 41 / 64。 | `碎页暗市` 实际展示并购买了 2 个香封候选；`残页补香` 将己形从 25 / 64 拉回 49 / 64，Boss 前 `终审前整册` 将己形从 42 / 64 拉回 64 / 64。样本仍紧，但能完成。 |

追加验证：

| 命令 / 检查 | 结果 |
|---|---|
| `npm run test -- src/tests/shopResolver.test.ts src/tests/routeResolver.test.ts src/tests/data.test.ts` | 通过，3 个测试文件、34 个测试。 |
| `npm run test -- src/tests/cardReachability.test.ts` | 全量测试先暴露 `拭宝札` 不再进入真实 reward offer；已将现有普通法宝支援牌纳入后段惊喜位后重跑通过，1 个测试文件、2 个测试。 |
| `npm run test` | 修复可达性回归后通过，33 个测试文件、235 个测试。 |
| `npm run typecheck` | 通过。 |
| `npm run lint` | 通过。 |
| `npm run build` | 通过；Vite 仍提示首包 chunk 超过 500 kB，沿用既有 warning。 |
| `git diff --check` | 通过。 |

结论：

- 衡简归册事件商店线已回到“紧但可完成”。
- 照微裂榜商店线在追加小调后已回到“紧但可完成”。这是一条真实浏览器样本，不等同于低 roll 穷举证明；后续仍可观察，但 T87 不再继续扩大范围。
- 本轮追加只处理照微裂榜商店线残余压力，没有扩大到稳行线削钱、秘传稀有度、完整随机地图或外层挑战模式。

## 后续建议

- T87 可以收口：两条高压线当前都回到“紧但可完成”。
- 稳行商店线、秘传稀有度、完整随机地图、Ascension / Daily / Custom 和 run 后图鉴 / 终册记录若继续推进，应另起任务编号。
