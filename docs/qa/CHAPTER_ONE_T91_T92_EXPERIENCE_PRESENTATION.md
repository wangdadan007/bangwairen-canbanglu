# T91-T92 首局理解、Boss 前准备与表现纵切 QA

日期：2026-05-25

## 1. 范围

本轮覆盖 T91 与 T92：

- T91：首局理解、商店 / 休整取舍、Boss 前准备摘要。
- T92：战斗桌案、卡牌框、敌人占位剪影、核心反馈动效和多视口浏览器冒烟。

本轮不新增卡牌、敌人、法宝、香封、Boss、章节、角色、生产依赖、第三方素材或正式音频。

## 2. T91 检查

新增 `RunReadinessPanel`，接入路线、商店和休整页面：

- 路线页在后段或 Boss 临近时显示“后段整册 / 临战整册”。
- 商店页显示“补给整册”，把己形、墨 / 香火钱、问名 / 防守、破形 / 临诏、香封、法宝 / 改榜合并到一屏。
- 休整页显示“休整整册”，用于对比修形、删牌、朱批和法宝维护的优先级。
- 摘要只展示当前状态和风险标签，不自动推荐出牌，不暴露奖励或路线内部权重。

首局短提示已补强：

- 商店提示补入香封“一次性、占槽、战斗内使用”的口径，同时保留“法宝不进牌堆”的旧契约。
- Boss 前商店提示强调香封、删牌、朱批、净卷四类准备入口。
- Boss 前休整提示强调己形、法宝反噬、删牌、朱批只能择重。

剩余 QA 风险：

- 当前是代码与浏览器冒烟级检查，不等同于真实新手外部试玩。
- 临战整册的阈值标签需要后续真实流程观察，确认不会让玩家误以为系统在强制推荐唯一解。

## 3. T92 检查

表现层本轮使用程序化 / CSS / Phaser 占位，不引入外部素材：

- Phaser 桌案从单一占位扩成残榜案面、朱砂压痕、三坛圆位、敌方剪影和响应式重绘。
- 战斗 HUD 的敌人面板新增占位剪影，按 Boss、小型窃香类、庙祝 / 吏员、青铜、纸马、坛位、火纸等类别区分。
- 手牌按钮按普通 / 精良 / 秘传、临诏、朱批状态增加框线和侧边提示。
- 关键反馈保留原有读屏区域，并增加破形 / 正名 / 伏诛归册 / 异动的短扫光与敌意图动效。
- 设置页关闭动画时，新增动效跟随 `.animations-off` 停止；系统 `prefers-reduced-motion` 仍兜底。

## 4. 浏览器覆盖

Playwright 冒烟已扩成三档桌面视口：

- 1280x720
- 1366x768
- 1920x1080

冒烟检查内容：

- 标题页、设置页、三角色新开局可达。
- 开局固定起始法宝可见。
- 战斗 HUD、敌方目标和结束回合按钮可见。
- Phaser canvas 可见，尺寸正常，舞台截图数据非空。
- 页面无水平溢出。
- 浏览器 error 级日志为空。

## 5. 验证记录

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run test`：通过，34 个测试文件、241 个测试。
- `npm run build`：通过；Vite 仍提示 Phaser 首包超过 500 kB，沿用既有风险记录。
- `npm run test:e2e`：普通沙箱因 `listen EPERM 127.0.0.1:5173` 失败；按沙箱规则提权重跑通过，3 个 Chromium 视口测试通过。
- `git diff --check`：通过。

## 6. 结论

T91 的第一轮目标已落地为“短提示 + 临战整册 + 页面取舍说明”，可以支持 Boss 前准备判断。

T92 已完成低成本表现纵切一期。T92A 已继续补充真实流程截图复核和小范围试玩取样准备，详见 `docs/qa/CHAPTER_ONE_T91_T92_REAL_FLOW_SCREENSHOT_REVIEW.md`。
